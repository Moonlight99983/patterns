#include <iostream>
#include <string.h>
#include <time.h>

using namespace std;

class waitnig
{
private:
	string* Wait;
	int maxLeght;
	int length;
public:
	Waitnig()
	{
		delete[]Wait;
	}
	Waitnig(int m)
	{
		this->maxLeght = m;
		this->length = 0;
		this->Wait = new string[maxLeght];
	}
	void Clear()
	{
		this->length = 0;
	}
	bool isFull
	{
		void add(int c)
	{
		if (inFull)
	{
			this->Wait(this->lenght++) = c;
		}

	}
	void Extract()
	{
		if(!IsEmpty())
		{
		string temp = this->Wait[0];
		
		+
 

   